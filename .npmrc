# Avoid phantom dependencies — keep node_modules flat for compatibility
# with tooling that expects npm-style resolution (e.g. Storybook, Vite).
shamefully-hoist=true

# Always save exact versions (no ^ or ~ in lockfile)
save-exact=false

# Use the registry configured here or in .npmrc for scoped packages
registry=https://registry.npmjs.org/
